apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  selector:
    app: mysql
  ports:
    - protocol: TCP
      port: {{ .Values.mysql.service.port | default 3306 | int }}
      targetPort: {{ .Values.mysql.service.port | default 3306 | int }}
